import { type StepType } from './types.js';

/**
 * BigData step types
 * Big data platforms: Hadoop, Spark, cloud storage
 */

export const BIGDATA_STEPS: Record<string, StepType> = {
	// BigData step types will be added here
};
