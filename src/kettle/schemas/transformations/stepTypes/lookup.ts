import { type StepType } from './types.js';

/**
 * Lookup step types
 * Data enrichment operations: lookups against reference data
 */

export const LOOKUP_STEPS: Record<string, StepType> = {
	// Lookup step types will be added here
};
