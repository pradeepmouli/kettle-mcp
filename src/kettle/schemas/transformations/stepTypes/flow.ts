import { type StepType } from './types.js';

/**
 * Flow control step types
 * Workflow control: switch, abort, block, delay
 */

export const FLOW_STEPS: Record<string, StepType> = {
	// Flow control step types will be added here
};
