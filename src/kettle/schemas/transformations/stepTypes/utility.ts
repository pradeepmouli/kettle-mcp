import { type StepType } from './types.js';

/**
 * Utility step types
 * Helper operations: variables, logging, data generation
 */

export const UTILITY_STEPS: Record<string, StepType> = {
	// Utility step types will be added here
};
