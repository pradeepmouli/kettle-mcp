import { type StepType } from './types.js';

/**
 * Scripting step types
 * Custom logic execution: JavaScript, Java, SQL scripts
 */

export const SCRIPTING_STEPS: Record<string, StepType> = {
	// Scripting step types will be added here
};
